export type Language = 'en' | 'vi';
export interface ProjectInput { brief:string; deadline:string; hoursPerDay:number; skills:string; constraints:string; language:Language }
export type ExecutionMethod = 'do_yourself'|'delegate_to_agent'|'read_documentation'|'follow_tutorial'|'use_example'|'human_review';
export interface AgentDelegationGuide { compatibleAgents:string[]; prompt:string; contextChecklist:string[]; expectedOutputs:string[]; reviewChecklist:string[] }
export interface PipelineTask { id:string; title:string; objective:string; techniqueIds:string[]; recommendedSoftwareId:string|null; softwareLabel:string; softwareAgnostic:boolean; method:ExecutionMethod; methodLabel:string; resourceIds:string[]; estimatedHumanMinutes:number; estimatedLearningMinutes:number; estimatedAgentMinutes:number; whyIncluded:string; definitionOfDone:string[]; agentDelegation:AgentDelegationGuide|null }
export interface ScoreBreakdown { requirements:number; familiarity:number; learningCost:number; executionCost:number; switchingCost:number; resourceQuality:number; deadlineFit:number; risk:number }
export interface CandidatePath { id:string; title:string; softwareIds:string[]; softwareLabels:string[]; tasks:PipelineTask[]; estimatedHumanMinutes:number; estimatedLearningMinutes:number; estimatedAgentMinutes:number; score:number; scoreBreakdown:ScoreBreakdown; strengths:string[]; weaknesses:string[]; viable:boolean; rejectionReasons:string[] }
export interface Solution { destination:string; detectedKind:string; capacityMinutes:number; daysAvailable:number; requirements:string[]; knownSoftware:string[]; recommended:CandidatePath; alternatives:CandidatePath[]; rejected:CandidatePath[]; skipped:string[]; solverVersion:string; scoringVersion:string }
