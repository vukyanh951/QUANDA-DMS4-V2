# Server-only Gemini configuration. Never prefix the API key with NEXT_PUBLIC_.
GEMINI_API_KEY=
GEMINI_MODEL=

# Canonical public origin used for metadata, for example the production Vercel domain.
SITE_URL=
